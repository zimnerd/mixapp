import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DistanceService {
  getDistanceBetween(userId: number, UserId:number): void {
    // Implement matchmaking logic
  }
}
